# Database Configuration
# Use "mysql" to switch to MySQL storage
STORAGE_TYPE=mysql

# MySQL database URL - replace with your own connection details
MYSQL_DATABASE_URL=mysql://root:password@localhost:3306/portfolio
