# Database Configuration
# Use either "postgres" or "mysql"
STORAGE_TYPE=postgres

# PostgreSQL database URL (used if STORAGE_TYPE=postgres)
# This is provided by Replit if you create a PostgreSQL database

# MySQL database URL (used if STORAGE_TYPE=mysql)
# MYSQL_DATABASE_URL=mysql://username:password@hostname:port/database_name
